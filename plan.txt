12号课设答辩，8号开始课设
课设计划为：
选题：定在猴子分类上，不做ui界面。不做展示图片。
训练次数调到200次，尺寸大小调整到300*400，把模型保留，画一下混淆矩阵，然后把图截了，这个模型暂时保留，注意读懂代码。
这是用的神经网络进行分类的，有序列化。

换成lvgg16模型来完成，实验写对比就行。

老师要求的图有了，在线上跑了，尺寸大小按要求了，目前主要就是读懂代码，换模型。
现在的任务就是把最终模型跑了，然后把基础模型花两个小时跑了，然后把数据得到，或许可以放到那里去看结果，把数据存起来，在那里看数据结果。ok,
今晚把东西整理好，睡前把电脑挂着跑两个小时。明天跑第二个模型。