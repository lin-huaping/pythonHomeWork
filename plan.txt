12号课设答辩，8号开始课设
课设计划为：
选题：定在猴子分类上，不做ui界面。不做展示图片。
训练次数调到200次，尺寸大小调整到400*400，把模型保留，画一下混淆矩阵，然后把图截了，这个模型暂时保留，注意读懂代码。
这是用的神经网络进行分类的，有序列化。

换成letnet5来完成。
或者vgg19多换几种模型来完成，实验写对比就行。

老师要求的图有了，在线上跑了，尺寸大小按要求了，目前主要就是读懂代码，换模型。